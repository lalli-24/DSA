def shiftGrid(grid,k):
    m=len(grid)
    n=len(grid[0])
    
    temp=grid[m-1][n-1]
    new=[]
    for z in range(k):
        for x in range(m):
            col=[]
            for y in range(n):
                temp1=grid[x][y]
                grid[x][y]=temp
                temp=temp1   
        temp=grid[m-1][n-1]
        
        return grid
print(shiftGrid([[1,2,3],[4,5,6],[7,8,9]], 1))
